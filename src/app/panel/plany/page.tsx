import { redirect } from "next/navigation";

/* Zakładka scalona z Zapotrzebowaniem w jeden panel Rekomendacje (2026-07-13). */
export default function PlanyRedirect() {
  redirect("/panel/zapotrzebowanie");
}
